{
  "type": "object",
  "required": ["event_id", "event_ts", "user_id", "event_type", "session_id"],
  "properties": {
    "event_id": { "type": "string" },
    "event_ts": { "type": "string" },
    "user_id": { "type": "string" },
    "event_type": { "type": "string", "enum": ["page_view", "add_to_cart", "purchase"] },
    "session_id": { "type": "string" },
    "product_id": { "type": ["string", "null"] },
    "price_gbp": { "type": ["number", "null"] }
  }
}
